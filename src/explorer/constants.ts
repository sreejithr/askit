import * as path from 'path';

export const EXTENSION_ROOT_DIR = path.join(__dirname, '..', '..');

export enum Icons {
    selectedUser = 'selected-user.svg',
    normalUser = 'normal-user.svg'
}
